const CartController = {
    // GET /cart
    index: (req, res) => {
        res.render('cart');
    }, 

    // POST /cart
    create: (req, res) => {
        
    }, 
}

module.exports = CartController;
